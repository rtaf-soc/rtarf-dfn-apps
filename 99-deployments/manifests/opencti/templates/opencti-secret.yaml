apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: opencti-secret
spec:
  refreshInterval: 1m
  secretStoreRef:
    kind: ClusterSecretStore
    name: k8s-secret-store      # name of the SecretStore (or kind specified)
  target:
    name: opencti-secret       # name of the k8s Secret to be created
  data:
  - secretKey: elasticsearch_username
    remoteRef:
      key: initial-secret
      property: ES_USER

  - secretKey: elasticsearch_password
    remoteRef:
      key: initial-secret
      property: ES_PASSWORD

  - secretKey: app__admin__email
    remoteRef:
      key: initial-secret
      property: OPENCTI_USER

  - secretKey: app__admin__password
    remoteRef:
      key: initial-secret
      property: OPENCTI_PASSWORD
